# Database configuration (contains credentials)
config/database.php

# Device integration config (Cashmatic / Ingenico / Epson / QR credentials & IPs)
config/devices.php

# Uploaded menu photos (user content; created on the server at runtime)
assets/uploads/menu/*
!assets/uploads/menu/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
